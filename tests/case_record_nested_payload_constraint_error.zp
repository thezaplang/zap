record User {
    id: Int,
}

enum Result { Empty, Value(User) }

record Response {
    result: Result,
}

fun read(response: Response) Int {
    case response {
        Response { result: Result.Value(User { id: 1 }) } { return 1; }
        else { return 0; }
    }
}
