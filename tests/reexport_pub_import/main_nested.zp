import "std/io" { printInt };
import "a/cos.zp";

fun main() Int {
  // Deep nested re-export chain:
  // main -> cos -> cos2 -> deeper/cos3 -> deeper/deepest/cos4 -> gleboko
  var value: Int = cos.gleboko();
  printInt(value);

  if value != 73 {
    return 1;
  }

  return 0;
}
